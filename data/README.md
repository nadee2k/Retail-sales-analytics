Project data directory

- Place the downloaded Superstore CSV dataset here as `raw_data.csv`.
- After running `notebooks/01_data_cleaning.ipynb`, a cleaned file will be written to `data/cleaned_data.csv`.

Notes:
- Keep raw data files out of commits unless necessary.
